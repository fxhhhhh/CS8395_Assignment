Rotate Array
def rotate(nums: list[int], k: int) -> None:
    """
    Rotate the array to the right by k steps, where k is non-negative.

    Parameters:
    - nums: List of integers
    - k: An integer

    Returns:
    - None. Modify the nums in-place instead.
    """
    pass
